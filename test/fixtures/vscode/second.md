# Second Document

This file exists so the suite can prove that the preview panel is reused (and
its content swapped) when a different document is previewed, instead of a
second webview panel being created.

## Marker

REUSED-PANEL-MARKER

Inline math stays available here too: $x = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a}$.
