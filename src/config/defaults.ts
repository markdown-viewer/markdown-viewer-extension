/**
 * Shared default render/layout settings.
 *
 * This file is now a thin re-export of the AUTO-GENERATED settings module.
 * The single source of truth is `settings-schema.json` — edit that file and
 * run `node scripts/sync-settings.js` to regenerate.
 *
 * Note: `language` (CLI) and `preferredLocale` (extension UI language) are
 * deliberately separate: one is the exported document language, the other
 * the extension's UI locale.
 */

export { DEFAULT_RENDER_SETTINGS } from './settings.generated.ts';
